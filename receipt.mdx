---
title: "Share-card prototype"
description: "An unsigned, manually created example. It is not part of the automatic call flow."
---

The [example share card](https://vibeconf.app/receipt/) is a browser renderer
for a small JSON object encoded in the URL fragment.

<Warning>
  This prototype is not automatically saved or posted by the Mac beta. It is not
  signed or verified. Anyone with the link can read and alter its contents. Do not
  include secrets, private transcript text, or private participant names.
</Warning>
